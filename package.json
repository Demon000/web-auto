{
  "private": true,
  "workspaces": {
    "packages": [
      "eslint-config",
      "eslint-config-node",
      "prettier-config",
      "logging",
      "android-auto-proto",
      "android-auto-ipc",
      "electron-ipc",
      "android-auto",
      "web",
      "electron"
    ]
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "prepare": "ts-patch install && npx typia patch && npm run prepare -w=electron",
    "dev": "tsc -b --watch"
  },
  "dependencies": {
    "typia": "^5.3.3"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.3.0",
    "@rushstack/eslint-patch": "^1.3.2",
    "@tsconfig/node20": "^20.1.1",
    "@types/native-duplexpair": "^1.0.0",
    "@types/node": "^20.4.7",
    "electron": "^28.0.0",
    "eslint": "^8.46.0",
    "ts-patch": "^3.0.2"
  }
}
